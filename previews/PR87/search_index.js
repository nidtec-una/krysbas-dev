var documenterSearchIndex = {"docs":
[{"category":"section","location":"solvers/#Solver-API","page":"Solvers","text":"Solvers are listed chronologically, by the publication year of the method each one implements.","title":"Solver API"},{"category":"section","location":"solvers/#GMRES-E","page":"Solvers","text":"","title":"GMRES-E"},{"category":"section","location":"solvers/#GMRES-DR","page":"Solvers","text":"","title":"GMRES-DR"},{"category":"section","location":"solvers/#LGMRES","page":"Solvers","text":"","title":"LGMRES"},{"category":"section","location":"solvers/#PD-GMRES","page":"Solvers","text":"","title":"PD-GMRES"},{"category":"section","location":"solvers/#SLGMRES-E-/-A-SLGMRES-E","page":"Solvers","text":"","title":"SLGMRES-E / A-SLGMRES-E"},{"category":"function","location":"solvers/#KrySBAS.gmres_e","page":"Solvers","text":"gmres_e(A, b; m=0, d=-1, tol=1e-6, maxit=0, x_initial=[], eigstol=1e-6)\n\nRestarted GMRES augmented with harmonic Ritz vectors (GMRES-E(m, d)).\n\nAt each restart, d approximate eigenvectors corresponding to the smallest eigenvalues of the search subspace are appended to the next Krylov subspace, accelerating convergence for matrices with a few small, problematic eigenvalues.\n\nArguments\n\nA: square coefficient matrix (sparse or dense, n×n)\nb::AbstractVector: right-hand side vector of length n\nm::Int=0: Krylov restart dimension; defaults to min(n, 10). Setting m == n dispatches to full unrestarted GMRES.\nd::Int=-1: number of harmonic Ritz vectors to append; defaults to min(m, 3). Setting d == 0 dispatches to standard restarted GMRES(m). The augmented subspace can exceed m+d when a harmonic Ritz value is complex (its real and imaginary parts are kept as two separate vectors, per [1]'s literal step 5); see the comment in the restart loop below for rationale.\ntol::Real=1e-6: relative residual tolerance for convergence\nmaxit::Int=0: maximum number of restart cycles; defaults to min(n, 10)\nx_initial::AbstractVector=[]: initial guess; defaults to the zero vector\neigstol::Real=1e-6: reserved for API compatibility (unused internally)\n\nReturns\n\nx: approximate solution vector\nflag::Bool: true if relresvec[end] < tol within maxit restarts\nrelresvec::Vector: relative residual norm after each restart cycle\nkdvec::Vector: Krylov subspace dimension used at each cycle\ntime::Float64: elapsed wall-clock time in seconds\n\nReferences\n\nMorgan, R. B. (1995). A restarted GMRES method augmented with eigenvectors. SIAM Journal on Matrix Analysis and Applications, 16(4), 1154–1171. doi:10.1137/S0895479893253975\n\n\n\n\n\n","title":"KrySBAS.gmres_e"},{"category":"function","location":"solvers/#KrySBAS.gmres_dr","page":"Solvers","text":"gmres_dr(A, b; m=0, k=-1, tol=1e-6, maxit=0, x_initial=[])\n\nRestarted GMRES with deflated (thick) restarting (GMRES-DR(m, k)).\n\nUnlike GMRES-E(m, d), which augments a fresh m-step Krylov subspace with d extra eigenvectors (total dimension m + d), GMRES-DR(m, k) keeps the subspace dimension fixed at m per cycle: the first keep basis vectors are recycled harmonic Ritz vectors (keep >= k, see below) and the remaining m - keep vectors come from standard Arnoldi.\n\nThe harmonic Ritz pairs are computed via the dense generalized eigenproblem F*y = λ*G*y (eigen(F, G)), since these matrices are small (m×m). When the k smallest-magnitude harmonic Ritz values include one half of a complex-conjugate pair, both the real and imaginary parts of that eigenvector are recycled instead of just one (equivalently keep = k + 1) — taking only the real part would not actually lie in the associated 2-D real invariant subspace and corrupts the thick restart's Arnoldi-consistency invariant.\n\nThe thick restart itself is a pure orthogonal basis rotation (Morgan 2002, Wu & Simon 1999): the recycled vectors Pk and the current residual direction are combined into an orthonormal (keep+1)-column basis Pkp1 via thin QR, and H, V are updated as H <- Pkp1'*H*Pk, V <- V*Pkp1. No matrix-vector products with A are needed for the recycle step.\n\nArguments\n\nA: square coefficient matrix (sparse or dense, n×n)\nb::AbstractVector: right-hand side vector of length n\nm::Int=0: subspace dimension per restart cycle; defaults to min(n, 10). Setting m == n dispatches to full unrestarted GMRES.\nk::Int=-1: number of harmonic Ritz vectors to recycle at each restart; defaults to min(m - 1, 3). Must satisfy 0 < k < m. Setting k == 0 dispatches to standard restarted GMRES(m).\ntol::Real=1e-6: relative residual tolerance for convergence\nmaxit::Int=0: maximum number of restart cycles; defaults to min(n, 10)\nx_initial::AbstractVector=[]: initial guess; defaults to the zero vector\n\nReturns\n\nx: approximate solution vector\nflag::Bool: true if relresvec[end] < tol within maxit restarts\nrelresvec::Vector: relative residual norm after each restart cycle\nkdvec::Vector: Krylov subspace dimension used at each cycle\ntime::Float64: elapsed wall-clock time in seconds\n\nReferences\n\nMorgan, R. B. (2002). GMRES with deflated restarting. SIAM Journal on Scientific Computing, 24(1), 20-37. doi:10.1137/S1064827599364659\n\nWu, K., & Simon, H. (2000). Thick-restart Lanczos method for large symmetric eigenvalue problems. SIAM Journal on Matrix Analysis and Applications, 22(2), 602-616.\n\n\n\n\n\n","title":"KrySBAS.gmres_dr"},{"category":"function","location":"solvers/#KrySBAS.lgmres","page":"Solvers","text":"lgmres(A, b; m=0, l=-1, tol=1e-6, maxit=0, x_initial=[])\n\nRestarted GMRES augmented with error approximation vectors (LGMRES(m, l)).\n\nAt each restart, up to l error approximation vectors from prior cycles are appended to the Krylov subspace, preserving information that standard restarted GMRES discards. This typically yields faster convergence than GMRES(m+l) at the same per-cycle cost.\n\nArguments\n\nA: square coefficient matrix (sparse or dense, n×n)\nb::AbstractVector: right-hand side vector of length n\nm::Int=0: base Krylov restart dimension; defaults to min(n, 10). Setting m == n dispatches to full unrestarted GMRES.\nl::Int=-1: maximum number of stored error vectors to append; defaults to 3. Setting l == 0 dispatches to standard restarted GMRES(m).\ntol::Real=1e-6: relative residual tolerance for convergence\nmaxit::Int=0: maximum number of restart cycles; defaults to min(n, 10)\nx_initial::AbstractVector=[]: initial guess; defaults to the zero vector\n\nReturns\n\nx: approximate solution vector\nflag::Bool: true if relresvec[end] < tol within maxit restarts\nrelresvec::Vector: relative residual norm after each restart cycle\nkdvec::Vector: Krylov subspace dimension used at each cycle\ntime::Float64: elapsed wall-clock time in seconds\n\nReferences\n\nBaker, A. H., Jessup, E. R., & Manteuffel, T. (2005). A technique for accelerating the convergence of restarted GMRES. SIAM Journal on Matrix Analysis and Applications, 26(4), 962–984. doi:10.1137/S0895479803422014\n\n\n\n\n\n","title":"KrySBAS.lgmres"},{"category":"function","location":"solvers/#KrySBAS.pd_gmres","page":"Solvers","text":"pd_gmres(A, b; m_initial=0, m_min_max=nothing, m_step=1,\n         tol=1e-6, maxit=0, x_initial=[], alpha_pd=[-3.0, 5.0])\n\nRestarted GMRES with a Proportional-Derivative controller (PD-GMRES(m)).\n\nThe restart parameter m is adjusted automatically each cycle by a PD control law driven by the recent residual history, avoiding the need to hand-tune a fixed restart value.\n\nArguments\n\nA: square coefficient matrix (sparse or dense, n×n)\nb::AbstractVector: right-hand side vector of length n\nm_initial::Int=0: initial restart dimension. 0 or n selects unrestarted GMRES; any value in 1:n-1 enables the PD-adaptive restarted mode.\nm_min_max::Union{Vector{Int},Nothing}=nothing: two-element vector [m_min, m_max] bounding the adaptive restart range; ignored when unrestarted. Defaults to [1, n].\nm_step::Int=1: increment/decrement step size for the PD rule\ntol::Real=1e-6: relative residual tolerance for convergence\nmaxit::Int=0: maximum number of restart cycles; defaults to ceil(n / m_initial) in restarted mode, or min(n, 10) unrestarted\nx_initial::AbstractVector=[]: initial guess; defaults to the zero vector\nalpha_pd::Vector{Float64}=[-3.0, 5.0]: PD gain vector [αₚ, α_d]\n\nReturns\n\nx: approximate solution vector\nflag::Bool: true if relresvec[end] < tol within maxit restarts\nrelresvec::Vector: relative residual norm after each restart cycle\nkdvec::Vector: restart dimension m used at each cycle\ntime::Float64: elapsed wall-clock time in seconds\n\nReferences\n\nNúñez, R. C., Schaerer, C. E., & Bhaya, A. (2018). A proportional-derivative control strategy for restarting the GMRES(m) algorithm. Journal of Computational and Applied Mathematics, 337, 209–224. doi:10.1016/j.cam.2018.01.009\n\n\n\n\n\n","title":"KrySBAS.pd_gmres"},{"category":"function","location":"solvers/#KrySBAS.slgmres_e","page":"Solvers","text":"slgmres_e(A, b; m=0, l=-1, d=-1, epsilon_threshold=0.01, tol=1e-6, maxit=0, x_initial=[], eigstol=1e-6)\n\nRestarted GMRES that switches, cycle by cycle, between two augmentation strategies based on an observed convergence-slowdown signal (SLGMRES-E(m, l, d)).\n\nBy default each cycle behaves like LGMRES(m, l): the restart subspace is augmented with up to l error approximation vectors from prior cycles. When slowdown is detected – the relative residual ratio between two consecutive cycles exceeds 1 - epsilon_threshold – it switches to GMRES-E(m, d) for one or more cycles: the subspace is instead augmented with d harmonic Ritz vectors computed from the cycle that triggered the detection. It switches back to LGMRES-style augmentation as soon as the ratio improves again.\n\nThe slowdown signal, epsilon in ‖r_m^(j)‖ / ‖r_m^(j-1)‖ = 1 - epsilon, is read directly off the Givens-rotated residual already computed for the least-squares solve every cycle – no extra cost. Under this switching rule the residual norm is provably non-increasing cycle to cycle (Theorem 2 of [1]).\n\nArguments\n\nA: square coefficient matrix (sparse or dense, n×n)\nb::AbstractVector: right-hand side vector of length n\nm::Int=0: restart parameter, fixed across all cycles; defaults to min(n, 10). Setting m == n dispatches to full unrestarted GMRES.\nl::Int=-1: number of error approximation vectors to append during LGMRES-style cycles; defaults to 3. Must satisfy l > 0.\nd::Int=-1: number of harmonic Ritz vectors to append during GMRES-E -style cycles; defaults to min(m, 3). Must satisfy d > 0.\nepsilon_threshold::Real=0.01: slowdown threshold. A cycle is classified as stagnating when ‖r^(j)‖ / ‖r^(j-1)‖ >= 1 - epsilon_threshold. Default matches the numerical experiments of [1].\ntol::Real=1e-6: relative residual tolerance for convergence\nmaxit::Int=0: maximum number of restart cycles; defaults to min(n, 10)\nx_initial::AbstractVector=[]: initial guess; defaults to the zero vector\neigstol::Real=1e-6: reserved for API compatibility (unused internally, since the underlying harmonic_ritz_vectors uses a dense eigensolver here, not an iterative one)\n\nReturns\n\nx: approximate solution vector\nflag::Bool: true if relresvec[end] < tol within maxit restarts\nrelresvec::Vector: relative residual norm after each restart cycle\nkdvec::Vector: Krylov subspace dimension used at each cycle (m during cycle 1, m+l or m+d thereafter depending on which augmentation was active)\ntime::Float64: elapsed wall-clock time in seconds\n\nReferences\n\nCabral, J. C., Schaerer, C. E., & Bhaya, A. (2020). Improving GMRES(m) using an adaptive switching controller. Numerical Linear Algebra with Applications, 27(5), e2305. doi:10.1002/nla.2305\n\n\n\n\n\n","title":"KrySBAS.slgmres_e"},{"category":"function","location":"solvers/#KrySBAS.a_slgmres_e","page":"Solvers","text":"a_slgmres_e(A, b; m_initial=0, m_min_max=nothing, m_step=1, l=-1, d=-1,\n            epsilon_threshold=0.01, alpha_pd=[2.0, 0.8], tol=1e-6, maxit=0,\n            x_initial=[], eigstol=1e-6)\n\nA-SLGMRES-E(mⱼ, l, d): extends slgmres_e with an adaptive restart parameter.\n\nWhenever a cycle triggers the same convergence-slowdown signal that switches slgmres_e from LGMRES-style to GMRES-E-style augmentation, this variant ALSO grows the restart parameter m via the proportional-derivative law of pd_rule (the same law used by pd_gmres), for as long as slowdown persists. m only changes on stagnating cycles; it stays fixed during LGMRES-style cycles. This is Algorithm 1 of [1].\n\nSee slgmres_e for the switching rule itself (eq. 33-35 of [1]) and why it can be read off the Givens-rotated residual at no extra cost.\n\nDeviation from the reference implementation: pd_rule's \"warm-up\" gating (it only applies the derivative term once at least 3 cycles of residual history exist, and only the proportional term with 2) is driven here by the complete, correctly-indexed cycle history. Cabral's own reference script (Adaptive_PD_lgmres_e.m) has a residual-history update for its first cycle commented out, which leaves its own cycle counter one cycle behind for the rest of the run. Both this port and the reference converge correctly; they simply grow m on a slightly different schedule.\n\nArguments\n\nA: square coefficient matrix (sparse or dense, n×n)\nb::AbstractVector: right-hand side vector of length n\nm_initial::Int=0: initial restart parameter; defaults to min(n, 10). Setting m_initial == n dispatches to full unrestarted GMRES.\nm_min_max::Union{Vector{Int},Nothing}=nothing: two-element vector [m_min, m_max] bounding m's adaptive range. Defaults to [1, n].\nm_step::Int=1: step size for growing the internal floor pd_rule falls back on when the PD law proposes m < m_min.\nl::Int=-1: number of error approximation vectors to append during LGMRES-style cycles; defaults to 3. Must satisfy l > 0.\nd::Int=-1: number of harmonic Ritz vectors to append during GMRES-E -style cycles; defaults to min(m_initial, 3). Must satisfy d > 0.\nepsilon_threshold::Real=0.01: slowdown threshold. A cycle is classified as stagnating when ‖r^(j)‖ / ‖r^(j-1)‖ >= 1 - epsilon_threshold.\nalpha_pd::Vector{Float64}=[2.0, 0.8]: proportional and derivative coefficients for the restart-parameter growth law (pd_rule). These are the values reported in [1] – note this differs from pd_gmres's own default of [-3.0, 5.0], which comes from a different paper ([2]) tuned for a different purpose (shrinking as well as growing m every cycle, rather than only growing it on detected stagnation).\ntol::Real=1e-6: relative residual tolerance for convergence\nmaxit::Int=0: maximum number of restart cycles; defaults to min(n, 10)\nx_initial::AbstractVector=[]: initial guess; defaults to the zero vector\neigstol::Real=1e-6: reserved for API compatibility (unused internally, since the underlying harmonic_ritz_vectors uses a dense eigensolver here, not an iterative one)\n\nReturns\n\nx: approximate solution vector\nflag::Bool: true if relresvec[end] < tol within maxit restarts\nrelresvec::Vector: relative residual norm after each restart cycle\nkdvec::Vector: Krylov subspace dimension used at each cycle (m during cycle 1, then m+l or m+d thereafter – with m itself possibly having grown – depending on which augmentation was active)\ntime::Float64: elapsed wall-clock time in seconds\n\nReferences\n\n[1] Cabral, J. C., Schaerer, C. E., & Bhaya, A. (2020). Improving GMRES(m) using an adaptive switching controller. Numerical Linear Algebra with Applications, 27(5), e2305. doi:10.1002/nla.2305\n\n[2] Núñez, R. C., Schaerer, C. E., & Bhaya, A. (2018). A proportional-derivative control strategy for restarting the GMRES(m) algorithm. Journal of Computational and Applied Mathematics, 337, 209–224. doi:10.1016/j.cam.2018.01.009\n\n\n\n\n\n","title":"KrySBAS.a_slgmres_e"},{"category":"section","location":"#KrySBAS.jl","page":"Home","text":"KrySBAS provides adaptive iterative solvers for sparse linear systems (Ax = b) based on Krylov subspaces.\n\nAll solvers share the same output signature:\n\nx, flag, relresvec, kdvec, time = solver(A, b; kwargs...)\n\nOutput Type Description\nx Vector Approximate solution\nflag Bool true if converged within maxit restarts\nrelresvec Vector Relative residual norm at each restart cycle\nkdvec Vector Krylov dimension used at each cycle\ntime Float64 Elapsed wall-clock time (seconds)","title":"KrySBAS.jl"},{"category":"section","location":"#Quick-start","page":"Home","text":"using KrySBAS\n\n# GMRES-E with m=27 and d=3 harmonic Ritz vectors\nx, flag, relresvec, kdvec, t = gmres_e(A, b; m=27, d=3, tol=1e-10)\n\n# GMRES-DR with m=27 Krylov steps and k=3 recycled Ritz vectors\nx, flag, relresvec, kdvec, t = gmres_dr(A, b; m=27, k=3, tol=1e-10)\n\n# LGMRES with m=30 Krylov steps and l=3 error vectors\nx, flag, relresvec, kdvec, t = lgmres(A, b; m=30, l=3, tol=1e-8)\n\n# PD-GMRES with adaptive restart starting at m=20\nx, flag, relresvec, kdvec, t = pd_gmres(A, b; m_initial=20, tol=1e-8)\n\n# SLGMRES-E, switching per cycle between LGMRES-style and GMRES-E-style augmentation\nx, flag, relresvec, kdvec, t = slgmres_e(A, b; m=27, l=3, d=3, tol=1e-10)\n\n# A-SLGMRES-E, additionally growing m via a PD law on stagnating cycles\nx, flag, relresvec, kdvec, t = a_slgmres_e(A, b; m_initial=27, l=3, d=3, tol=1e-10)","title":"Quick start"},{"category":"section","location":"#Solvers","page":"Home","text":"Listed chronologically, by the publication year of the method each one implements.\n\nSolver Description\ngmres_e Restarted GMRES augmented with harmonic Ritz vectors (Morgan, 1995)\ngmres_dr Restarted GMRES with deflated (thick) restarting (Morgan, 2002)\nlgmres Restarted GMRES augmented with error approximation vectors (Baker, Jessup & Manteuffel, 2005)\npd_gmres Restarted GMRES with PD-controller restart adaptation (Núñez, Schaerer & Bhaya, 2018)\nslgmres_e Restarted GMRES that switches, cycle by cycle, between LGMRES-style and GMRES-E-style augmentation (Cabral, Schaerer & Bhaya, 2020)\na_slgmres_e slgmres_e plus PD-adaptive restart growth on stagnating cycles (Cabral, Schaerer & Bhaya, 2020)","title":"Solvers"}]
}
